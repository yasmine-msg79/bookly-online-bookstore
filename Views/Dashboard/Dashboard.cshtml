@* @model BookStore.ViewModel.AdminDashboardViewModel

@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container my-4">
    <h1 class="mb-4 text-primary">Admin Dashboard</h1>

    <!-- Summary Cards -->
    <div class="row g-4">
        <div class="col-md-4">
            <div class="card text-white bg-primary h-100 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Books</h5>
                    <p class="display-5 fw-bold">@Model.TotalBooks</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card text-white bg-success h-100 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Stock</h5>
                    <p class="display-5 fw-bold">@Model.TotalStock</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card text-white bg-warning h-100 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Recent Orders (30 days)</h5>
                    <p class="display-5 fw-bold">@Model.RecentOrdersCount</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Top Selling Books List -->
    <div class="mt-5">
        <h3>Top Selling Books Details</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Stock</th>
                        <th>Total Sold</th>
                        <th>Average Rating</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var book in Model.TopSellingBooks)
                    {
                        <tr>
                            <td>@book.Title</td>
                            <td>@book.Author</td>
                            <td>@book.StockQuantity</td>
                            <td>@book.TotalSold</td>
                            <td>@string.Format("{0:F1}", book.AverageRating)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- All Books List -->
    <div class="mt-5">
        <h3>All Books</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Stock</th>
                        <th>Total Sold</th>
                        <th>Average Rating</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var book in Model.AllBooks)
                    {
                        <tr>
                            <td>@book.Title</td>
                            <td>@book.Author</td>
                            <td>@book.StockQuantity</td>
                            <td>@book.TotalSold</td>
                            <td>@string.Format("{0:F1}", book.AverageRating)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>


    <!-- Users Management -->
    <div class="mt-5">
        <h3>Users</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Suspended</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.Users)
                    {
                        <tr>
                            <td>@user.FullName</td>
                            <td>@user.Email</td>
                            <td>@user.Role</td>
                            <td>@(user.IsSuspended ? "Yes" : "No")</td>
                            <td>
                                <a asp-controller="Users" asp-action="Suspend" asp-route-id="@user.Id" class="btn btn-sm btn-danger">Suspend</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- Categories -->
    <div class="mt-5 mb-5">
        <h3>Categories</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Name</th>
                        <th>Book Count</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model.Categories)
                    {
                        <tr>
                            <td>@category.Name</td>
                            <td>@category.BookCount</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
 *@

 @model BookStore.ViewModel.AdminDashboardViewModel

@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="container my-4">
    <h1 class="mb-4 text-primary">Admin Dashboard</h1>

    <!-- Summary Cards -->
    <div class="row g-4">
        <div class="col-md-4">
            <div class="card text-white bg-primary h-100 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Books</h5>
                    <p class="display-5 fw-bold">@Model.TotalBooks</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card text-white bg-success h-100 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Stock</h5>
                    <p class="display-5 fw-bold">@Model.TotalStock</p>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card text-white bg-warning h-100 shadow-sm">
                <div class="card-body text-center">
                    <h5 class="card-title">Recent Orders (30 days)</h5>
                    <p class="display-5 fw-bold">@Model.RecentOrdersCount</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Top Selling Books List -->
    <div class="mt-5">
        <h3>Top Selling Books Details</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Stock</th>
                        <th>Total Sold</th>
                        <th>Average Rating</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var book in Model.TopSellingBooks)
                    {
                        <tr>
                            <td>@book.Title</td>
                            <td>@book.Author</td>
                            <td>@book.StockQuantity</td>
                            <td>@book.TotalSold</td>
                            <td>@string.Format("{0:F1}", book.AverageRating)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- All Books List -->
    <div class="mt-5">
        <h3>All Books</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Title</th>
                        <th>Author</th>
                        <th>Stock</th>
                        <th>Total Sold</th>
                        <th>Average Rating</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var book in Model.AllBooks)
                    {
                        <tr>
                            <td>@book.Title</td>
                            <td>@book.Author</td>
                            <td>@book.StockQuantity</td>
                            <td>@book.TotalSold</td>
                            <td>@string.Format("{0:F1}", book.AverageRating)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- Users Management -->
    <div class="mt-5">
        <h3>Users</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Suspended</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var user in Model.Users)
                    {
                        <tr>
                            <td>@user.FullName</td>
                            <td>@user.Email</td>
                            <td>@user.Role</td>
                            <td>@(user.IsSuspended ? "Yes" : "No")</td>
                            <td>
                                <form asp-controller="Users" asp-action="Suspend" asp-route-id="@user.Id" method="post" style="display:inline">
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        @(user.IsSuspended ? "Unsuspend" : "Suspend")
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <!-- Categories -->
    <div class="mt-5 mb-5">
        <h3>Categories</h3>
        <div class="table-responsive shadow-sm">
            <table class="table table-striped table-hover align-middle">
                <thead class="table-dark">
                    <tr>
                        <th>Name</th>
                        <th>Book Count</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model.Categories)
                    {
                        <tr>
                            <td>@category.Name</td>
                            <td>@category.BookCount</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
